[project]
name = "lofi-cli"
version = "0.1.2"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
lofi = "lofi.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"