[project]
name = "lofi-cli"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
lofi = "main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"